#include "rectangleButton.h"

// RectangleButton::Button(Vector2 position, Vector2 size) : position(position), size(size)
// {

// }

// bool RectangleButton::isPressed() const
// {
//     Vector2 mouse_pos = GetMousePosition();
//     bool collision = CheckCollisionPointRec(mouse_pos, Rectangle{ position.x, position.y, size.x, size.y });
//     bool pressed = IsMouseButtonReleased(MOUSE_BUTTON_LEFT);
//     return collision && pressed;
// }